# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp

# Cache — large data files (re-fetched on demand)
cache/*.pkl
cache/*.parquet
cache/*.csv
cache/*.json.bak

# Keep prediction JSON files (needed by dashboard)
!cache/mlb_predictions.json
!cache/soccer_predictions.json
# Keep manually added fixtures
!cache/soccer_fixtures.csv

# Claude Code settings (local only)
.claude/

# Trained models (large binary files — re-train after clone)
# models_saved/ is intentionally tracked (small enough to include)

# OS
.DS_Store
Thumbs.db

# Secrets / env
.env
*.env
